import classes from "./Footer.module.css";

const Footer = () => {
  return (
    <nav className={classes.footer}>
      <div>
        <span>Project Review App &copy; Inc.</span>
      </div>
    </nav>
  );
};

export default Footer;
